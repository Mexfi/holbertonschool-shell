#!/bin/usr/ver bash
# This script displays a list of currently running processes with hierarchy.

echo "Currently Running Processes (Hierarchical View):"
echo "------------------------------------------------"

# Use ps -axjf for a full listing with a job control format, including
# processes without a controlling terminal and showing hierarchy.
# The 'f' option provides ASCII art process tree.
ps -axjf
