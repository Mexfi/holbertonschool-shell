#!/usr/bin/env bash
# Kill the process 7-highlander
PID=$(pgrep -f 7-highlander)

# Check if the process was found
if [ -n "$PID" ]; then
    # Kill the process
    kill $PID
    echo "Killed 7-highlander process (PID: $PID)"
else
    echo "7-highlander process not found"
fi
