#!/usr/bin/env bash
# This script displays the content of the current directory, showing only the part after the first dash.

for file in *-*; do
    # Only process regular files (ignore directories or hidden files)
    if [ -f "$file" ]; then
        # Display the part after the first dash
        echo "${file#*-}"
    fi
done

