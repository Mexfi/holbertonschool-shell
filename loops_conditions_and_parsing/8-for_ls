#!/usr/bin/env bash
# This script displays the content of the current directory, showing only the part of the filename after the first dash

for file in *-*  # Loop over all files that contain a dash
do
  # Display the part of the filename after the first dash
  echo "${file#*-}"
done

