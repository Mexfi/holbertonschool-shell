#!/usr/bin/env bash
# This script displays files in the current directory, showing only the part after the first dash

for file in *-*  # Process only files containing a dash in their name
do
  echo "${file#*-}"  # Display part of the filename after the first dash
done

